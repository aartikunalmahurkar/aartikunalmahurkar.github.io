# Aarti Kunal Mahurkar Portfolio — V7 Fixed

Fixes in this build:
- quote panel is forced in front with explicit z-index and no longer sits behind the photo
- portrait is no longer aggressively zoomed/cropped
- real 4K-upscaled photo is used as foreground
- office-style background is recreated with CSS so the portrait itself is not blurred
- LinkedIn remains included
- chatbot remains included

Important photo note:
The source photo in the resume is very small. `aarti-photo-4k.jpg` is a 4K-class upscale, but true optical detail cannot be recovered from the PDF source. For genuinely sharp 4K quality, replace it later with the original phone/camera photo using the same filename.

Deploy by replacing all files in the GitHub Pages repository root, then Ctrl+F5.
